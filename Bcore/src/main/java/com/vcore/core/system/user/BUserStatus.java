package com.vcore.core.system.user;

/**
 * Enumeration of possible statuses for a virtual user in the BlackBox environment.
 */
public enum BUserStatus {
    /** The user is active and available for use. */
    ENABLE,

    /** The user is disabled and cannot be used. */
    DISABLE
}
